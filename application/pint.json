{
    "preset": "per",
    "rules": {
        "ordered_imports": {
            "sort_algorithm": "alpha"
        }
    },
    "paths": [
        "src",
        "tests"
    ],
    "exclude": [
        "var",
        "vendor"
    ]
}

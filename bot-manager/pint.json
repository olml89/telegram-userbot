{
    "preset": "per",
    "rules": {
        "ordered_imports": {
            "sort_algorithm": "alpha"
        }
    },
    "exclude": [
        "docker",
        "supervisor",
        "var",
        "vendor"
    ]
}

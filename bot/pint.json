{
    "preset": "per",
    "rules": {
        "ordered_imports": {
            "sort_algorithm": "alpha"
        }
    },
    "exclude": [
        "supervisor",
        "var",
        "vendor"
    ]
}
